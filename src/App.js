import React from 'react'
import { GridButton } from './components/GridButton';

export const App = () => {
    return (
        <div className="container">
            <header>
                <h1>Buttons</h1>
            </header>

            <GridButton />
        </div>
    )
}
